{
  "name": "finance-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API do FINANCE (Fase 6) — Node.js + Express + SQLite (node:sqlite), autenticação por e-mail/senha com JWT.",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test:security": "node scripts/security-smoke.mjs"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.7.0",
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
